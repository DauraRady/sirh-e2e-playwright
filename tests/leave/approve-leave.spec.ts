import { test, expect } from '../../fixtures/base.fixture';

test.describe('Leave - Approve', () => {
  test.skip('should allow admin to approve pending leave request', async ({ page }) => {
    // TODO: requires a pending leave request to exist
    // This test will navigate to Leave List and approve a request
  });
});
